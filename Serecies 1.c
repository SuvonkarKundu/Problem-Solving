#include<stdio.h>
using namespace std;
class a
{
    int x,h,n;
    void input(void)
    {
        cin>>x;
        cin>>n;
    }
    void output(void)
    {
        cout<<serices();
    }
    int fac(int b);
    int pow(int a);
    int serices(int l);
};
int A::fac(int b)
{
    int h=1;
    for(i=1;i<=n;i++)
    {
        h=h*i
        return(h);
    }
}

int A::pow(int a)
{
    int q;
    for(i=1;i<=n;i++)
    {
        q=i*i;
        return(q);
    }
}
int A::serices(int l)
{
    Z=pow()/fac();
    return(z);
}
int main()
{
    w ob1;
    ob1.input();
    ob1.output();
}




















